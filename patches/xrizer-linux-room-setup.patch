diff --git a/src/openxr_data.rs b/src/openxr_data.rs
index 9511cc8..ebc07c7 100644
--- a/src/openxr_data.rs
+++ b/src/openxr_data.rs
@@ -7,7 +7,11 @@ use glam::f32::{Quat, Vec3};
 use log::{info, warn};
 use openvr as vr;
 use openxr as xr;
+use serde::Deserialize;
 use std::mem::ManuallyDrop;
+use std::path::PathBuf;
+#[cfg(all(not(test), not(feature = "static-openxr")))]
+use std::path::Path;
 use std::sync::{
     RwLock,
     atomic::{AtomicI64, Ordering},
@@ -105,14 +109,111 @@ fn make_version() -> u32 {
         + env!("CARGO_PKG_VERSION_PATCH").parse::<u32>().unwrap_or(1)
 }
 
+/// The PSVR2 SteamVR driver bridge bakes the saved chaperone transform into
+/// every device pose.  OpenVR RawAndUncalibrated coordinates must omit that
+/// transform, because the Toolkit's passthrough camera poses are still in the
+/// driver's raw tracking universe.  A STAGE-derived space with this pose
+/// removes the baked transform when views and devices are located in it.
+fn raw_tracking_space_pose() -> xr::Posef {
+    if std::env::var_os("XRIZER_FORCE_RAW_TRACKING_SPACE").is_none() {
+        return xr::Posef::IDENTITY;
+    }
+
+    #[derive(Deserialize)]
+    struct ChaperoneInfo {
+        universes: Vec<Universe>,
+    }
+
+    #[derive(Deserialize)]
+    struct Universe {
+        standing: Standing,
+    }
+
+    #[derive(Deserialize)]
+    struct Standing {
+        translation: [f32; 3],
+        yaw: f32,
+    }
+
+    let path = std::env::var_os("XRIZER_RAW_CHAPERONE_PATH")
+        .map(PathBuf::from)
+        .or_else(|| {
+            std::env::var_os("STEAM_ROOT").map(|root| {
+                PathBuf::from(root)
+                    .join("config/playstation_vr2/chaperone_info.vrchap")
+            })
+        })
+        .or_else(|| {
+            std::env::var_os("HOME").map(|home| {
+                PathBuf::from(home)
+                    .join(".local/share/Steam/config/playstation_vr2/chaperone_info.vrchap")
+            })
+        });
+
+    let Some(path) = path else {
+        warn!("Raw tracking space requested, but no Steam configuration path is available");
+        return xr::Posef::IDENTITY;
+    };
+    let data = match std::fs::read(&path) {
+        Ok(data) => data,
+        Err(error) if error.kind() == std::io::ErrorKind::NotFound => {
+            info!("No saved PSVR2 chaperone transform; raw space is identity");
+            return xr::Posef::IDENTITY;
+        }
+        Err(error) => {
+            warn!("Couldn't read PSVR2 chaperone data from {}: {error}", path.display());
+            return xr::Posef::IDENTITY;
+        }
+    };
+    let info: ChaperoneInfo = match serde_json::from_slice(&data) {
+        Ok(info) => info,
+        Err(error) => {
+            warn!("Couldn't parse PSVR2 chaperone data from {}: {error}", path.display());
+            return xr::Posef::IDENTITY;
+        }
+    };
+    let Some(standing) = info.universes.first().map(|universe| &universe.standing) else {
+        warn!("PSVR2 chaperone data has no universe; raw space is identity");
+        return xr::Posef::IDENTITY;
+    };
+
+    // This intentionally mirrors steamvr_lh/device.cpp::init_chaperone.
+    let orientation = Quat::from_axis_angle(Vec3::NEG_Y, standing.yaw);
+    let position = orientation * Vec3::from_array(standing.translation);
+    info!(
+        "Raw tracking space removes PSVR2 chaperone yaw {:.6} rad and translation [{:.6}, {:.6}, {:.6}]",
+        standing.yaw, position.x, position.y, position.z
+    );
+    xr::Posef {
+        orientation: xr::Quaternionf {
+            x: orientation.x,
+            y: orientation.y,
+            z: orientation.z,
+            w: orientation.w,
+        },
+        position: xr::Vector3f {
+            x: position.x,
+            y: position.y,
+            z: position.z,
+        },
+    }
+}
+
 impl<C: Compositor> OpenXrData<C> {
     pub fn new(injector: &Injector) -> Result<Self, InitError> {
         #[cfg(all(not(test), feature = "static-openxr"))]
         let entry = xr::Entry::linked();
 
         #[cfg(all(not(test), not(feature = "static-openxr")))]
-        let entry = unsafe { xr::Entry::load() }
-            .expect("Failed to load OpenXR loader — is libopenxr-loader installed?");
+        // Fedora/Bazzite's runtime package intentionally ships the loader by
+        // its SONAME (libopenxr_loader.so.1) without the unversioned symlink
+        // from the -devel package. Try the portable name first, then the
+        // standard Linux SONAME used by packaged applications.
+        let entry = unsafe {
+            xr::Entry::load()
+                .or_else(|_| xr::Entry::load_from(Path::new("libopenxr_loader.so.1")))
+        }
+        .expect("Failed to load OpenXR loader — is libopenxr-loader installed?");
 
         #[cfg(test)]
         let entry =
@@ -166,11 +267,23 @@ impl<C: Compositor> OpenXrData<C> {
             .system(xr::FormFactor::HEAD_MOUNTED_DISPLAY)
             .map_err(InitError::SystemCreationFailed)?;
 
+        // PSVR2Toolkit.UnitySetup publishes passthrough camera poses in the
+        // driver's raw coordinate system. Its Unity SteamVR helper cannot
+        // call SetTrackingSpace after the OpenVR loader has already created
+        // this xrizer instance, so allow its launcher to select raw space at
+        // instance creation. Keep the normal standing origin for every other
+        // application.
+        let initial_origin = if std::env::var_os("XRIZER_FORCE_RAW_TRACKING_SPACE").is_some() {
+            vr::ETrackingUniverseOrigin::RawAndUncalibrated
+        } else {
+            vr::ETrackingUniverseOrigin::Standing
+        };
+
         let session_data = SessionReadGuard(RwLock::new(ManuallyDrop::new(
             SessionData::new(
                 &instance,
                 system_id,
-                vr::ETrackingUniverseOrigin::Standing,
+                initial_origin,
                 None,
             )?
             .0,
@@ -434,6 +547,7 @@ pub struct SessionData {
     local_space_adjusted: xr::Space,
     stage_space_reference: xr::Space,
     stage_space_adjusted: xr::Space,
+    raw_space: xr::Space,
     pub current_origin: vr::ETrackingUniverseOrigin,
 
     pub input_data: crate::input::InputSessionData,
@@ -539,6 +653,12 @@ impl SessionData {
                 .create_reference_space(xr::ReferenceSpaceType::STAGE, xr::Posef::IDENTITY)
                 .unwrap()
         });
+        let raw_space = session
+            .create_reference_space(
+                xr::ReferenceSpaceType::STAGE,
+                raw_tracking_space_pose(),
+            )
+            .unwrap();
 
         let mut buf = xr::EventDataBuffer::new();
         loop {
@@ -571,6 +691,7 @@ impl SessionData {
                 local_space_adjusted,
                 stage_space_reference,
                 stage_space_adjusted,
+                raw_space,
                 input_data: Default::default(),
                 comp_data: Default::default(),
                 overlay_data: Default::default(),
@@ -637,8 +758,7 @@ impl SessionData {
             vr::ETrackingUniverseOrigin::Seated => &self.local_space_adjusted,
             vr::ETrackingUniverseOrigin::Standing => &self.stage_space_adjusted,
             vr::ETrackingUniverseOrigin::RawAndUncalibrated => {
-                crate::warn_unimplemented!("RawAndUncalibrated tracking space");
-                &self.stage_space_reference
+                &self.raw_space
             }
         }
     }
@@ -648,6 +768,11 @@ impl SessionData {
         match ty {
             xr::ReferenceSpaceType::VIEW => &self.view_space,
             xr::ReferenceSpaceType::LOCAL => &self.local_space_adjusted,
+            xr::ReferenceSpaceType::STAGE
+                if self.current_origin == vr::ETrackingUniverseOrigin::RawAndUncalibrated =>
+            {
+                &self.raw_space
+            }
             xr::ReferenceSpaceType::STAGE => &self.stage_space_adjusted,
             other => panic!("Unsupported reference space type: {other:?}"),
         }
@@ -659,7 +784,6 @@ impl SessionData {
             vr::ETrackingUniverseOrigin::Seated => xr::ReferenceSpaceType::LOCAL,
             vr::ETrackingUniverseOrigin::Standing => xr::ReferenceSpaceType::STAGE,
             vr::ETrackingUniverseOrigin::RawAndUncalibrated => {
-                crate::warn_unimplemented!("RawAndUncalibrated tracking space");
                 xr::ReferenceSpaceType::STAGE
             }
         }
