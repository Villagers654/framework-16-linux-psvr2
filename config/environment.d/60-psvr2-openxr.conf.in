# Allow Steam to import whichever OpenXR runtime currently owns the active
# headset. PSVR2 and standalone streamers claim it only while they are active.
PRESSURE_VESSEL_IMPORT_OPENXR_1_RUNTIMES=1
PRESSURE_VESSEL_FILESYSTEMS_RW=${XDG_RUNTIME_DIR}/monado_comp_ipc
